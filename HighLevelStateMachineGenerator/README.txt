Alejandro Alvarez (aalvarez1) Ian Hooks (Ihooks) Mubarak Hassan (mubarakhassan) Matt Heger (heger)

ECE 474

The program uses command line arguments to read an input file, parse the text into verilog commands and writes the verilog commands to a .v file.

This creates a high level state machine that is synthesizeble in verilog! 